# Cloudflare Workers 設定 — cowork-agent-kintone-mcp
#
# マルチテナント版 (Phase 1b-3 の改訂):
# - vars / secret は一切不要
# - kintone domain は URL パス /mcp/<domain> から受け取る
# - OAuth client / Anthropic key はリクエストヘッダから受け取る
#
# Deploy:
#   wrangler login && wrangler deploy

name = "cowork-agent-kintone-mcp"
main = "src/index.ts"
compatibility_date = "2026-04-01"
